{
    "name": "@prefigure/prefig-wasm",
    "version": "0.7.0",
    "private": true,
    "description": "PreFigure compiled to WebAssembly",
    "license": "GPL-3.0-only",
    "scripts": {
        "build": "wasm-pack build --target nodejs --out-dir pkg",
        "test": "npm run build && vitest run",
        "test:only": "vitest run"
    },
    "devDependencies": {
        "vitest": "^2.1.8"
    }
}
